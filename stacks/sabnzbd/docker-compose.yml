---
services:
  sabnzbd:
    image: lscr.io/linuxserver/sabnzbd:latest
    container_name: sabnzbd
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f 'http://localhost:8080/api?mode=version&apikey=${SABNZBD_API_KEY}' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - traefik
    ports:
      - 10000:8080
    volumes:
      - /opt/stacks/sabnzbd:/config
      - /mnt/nas/data/usenet:/data/usenet
      - /opt/stacks/sabnzbd/scripts:/scripts
    environment:
      - PUID=1000
      - PGID=1000
      - INSTALL_PACKAGES=clamav-daemon
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      - "traefik.http.routers.sabnzbd.entrypoints=websecure"
      - "traefik.http.routers.sabnzbd.middlewares=internal@file,secure-headers@file"
      - "traefik.http.routers.sabnzbd.tls.certresolver=cloudflare"
      - "traefik.http.routers.sabnzbd.rule=Host(`sabnzbd.${DOMAIN}`)"
      - "traefik.http.services.sabnzbd.loadbalancer.server.port=8080"

networks:
  traefik:
    external: true
