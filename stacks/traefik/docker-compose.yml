---
services:
  traefik:
    image: traefik:latest
    container_name: traefik
    restart: unless-stopped
    depends_on:
      docker-socket-proxy:
        condition: service_healthy
    networks:
      - socket-proxy
      - traefik
    ports:
      - "80:80"
      - "443:443"
    security_opt:
      - no-new-privileges:true
    command:
      - --configFile=/traefik.yml
      - --certificatesresolvers.cloudflare.acme.email=${ACME_EMAIL}
      - --certificatesresolvers.cloudflare.acme.dnschallenge.propagationtimeout=600s
      - --certificatesresolvers.cloudflare.acme.dnschallenge.pollinginterval=10s
      - --log.level=DEBUG
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./traefik.yml:/traefik.yml:ro
      - /opt/stacks/traefik/acme.json:/acme.json
      - ./config:/config:ro
    environment:
      - CLOUDFLARE_DNS_API_TOKEN=${CLOUDFLARE_DNS_API_TOKEN}
      - ACME_EMAIL=${ACME_EMAIL}
      - DOMAIN=${DOMAIN}
      - TZ=${TZ}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.entrypoints=http"
      - "traefik.http.routers.traefik.rule=Host(`traefik.${DOMAIN}`)"
      - "traefik.http.routers.traefik-secure.entrypoints=websecure"
      - "traefik.http.routers.traefik-secure.rule=Host(`traefik.${DOMAIN}`)"
      - "traefik.http.routers.traefik-secure.tls.certresolver=cloudflare"
      - "traefik.http.routers.traefik-secure.tls.domains[0].main=${DOMAIN}"
      - "traefik.http.routers.traefik-secure.tls.domains[0].sans=*.${DOMAIN}"
      - "traefik.http.routers.traefik-secure.middlewares=secure-headers@file"
      - "traefik.http.routers.traefik-secure.service=api@internal"

networks:
  traefik:
    external: true
  socket-proxy:
    external: true
